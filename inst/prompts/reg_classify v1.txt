You are an economic research assisstant that helps to extract information from scientific articles. You work very carefully.

Attached is an {{doc_type_descr}}. We want to extract information from all regression shown in tables in {{cur_doc}}. Here is a list of the tables whose regressions we want to analyze.

LIST OF CONSIDERED TABLES

{{tab_list}}

Attached is an HTML file that contains an HTML representation of all tables in {{cur_doc}} each of those tables, with each <td> and <th> cell identified with a unique cell id. 

Also a attached is a txt file that contains the line numbered Stata source code of the that are part of the article's reproduction package. For regression commands also the regression output is shown.


A previous analysis identified the separate regressions. The following JSON array shows all those identified regressions. Each regression has a unique ID given by the field regid.

LIST OF IDENTIFIED REGRESSIONS

{{reg_list}}

Your task is the following: Go through every regression in the JSON array above and characterize it according to the JSON response schema shown below.

Every regression in the list above should have one entry in the generated JSON array with the same regid.

If for some field in the response schema, you don't know the answer given the provided information, please set the field value to null.

JSON RESPONSE SCHEMA:
{
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "tabid": {
        "type": ["string"],
        "description": "The table ID as stated in the list of regressions above."
      },
      "regid": {
        "type": ["string"],
        "description": "The regression ID as stated in the field 'regid' in the list of regressions shown above."
      },
      "short_descr": {
        "type": "string",
        "description": "A short description of what the regression analyzes based on the information in the article."
      },
      "is_did_reg": {
        "type": ["boolean", "null"],
        "description": "true if the regression performs a difference-in-difference (DID) analysis."
      },
      "is_rdd_reg": {
        "type": ["boolean", "null"],
        "description": "true if the regression performs a regression discontinuity design (RDD) analysis."
      },
      "is_iv_reg": {
        "type": ["boolean", "null"],
        "description": "true if it is an instrumental varibale regression"
      },
      "is_iv_first_stage_reg": {
        "type": ["boolean", "null"],
        "description": "true if the shown regression results correspond to the first stage regression of an instrumental variable regression (in the script the command could be an iv regression with the option to show first stage results or it could be a separate OLS first stage regression)"
      },
      "is_placebo_test": {
        "type": ["boolean", "null"],
        "description": "true if the regression performs a placebo test, or a similar permutation test."
      },
      "is_pref_spec_in_tab": {
        "type": ["boolean", "null"],
        "description": "Often tables show multiple regression specifications and sometimes the authors state which specification is their preferred specification. Set true if this regression is the preferred specification  among the specifications shown in the table."
      },
      "is_main_result": {
        "type": ["boolean", "null"],
        "description": "true if the regression results are described as main results of the article (compared to robustness checks or additional results)"
      },
      "is_additional_result": {
        "type": ["boolean", "null"],
        "description": "true if the regression shows additional results that are not described as main results of the article"
      },
      "is_robustness_check": {
        "type": ["boolean", "null"],
        "description": "true if the regression is mainly a robustness check for other results."
      },
      "label_dep_var": {
        "type": ["string", "null"],
        "description": "Based on the information in the article and table find a suitable label for the dependent variable in the regression."
      },
      "labels_coef_of_interest": {
        "type": ["string", "null"],
        "description": "Often regression tables show both coefficients of primary interest for the analysis and coefficients for control variables that are not of primary interest. Sometimes only coefficient of primary interest are shown. Please state the variable lables of the coefficients of primary interest for this regression as shown in the table. If there are multiple variables of primary interest your string shall be a comma separted list, e.g. 'age,gender'."
      },
      "cell_id_coef_of_interest": {
        "type": ["string", "null"],
        "description": "Please state the cell ids of all cells for this regression that show the numeric value of the  coefficient of interests or their standard error / p-value / t-value. Return a comma separated list of all those cell_ids, like 'c2_10,c2_12'."
      },
      "analyses_heterogeneity": {
        "type": ["boolean", "null"],
        "description": "true if the  cofficient of interest of the regressions analyze heterogenous effects, e.g. if the regression provided information on how treatment effect sizes differ between subgroups."
      },
      "map_label_to_var": {
        "type": "array",
        "description": "The labels for dependent or explanatory variables shown used in the article or appendix usually differs from the corresponding variable name in the data set and Stata code. In this array you are asked to map for the current regression only, the dependent variable and for every explanatory variable that is shown in the table the label from the article / appendix to the variable name in the data set and used in the Stata code. Explanatory variables that have no label or description in the table for this regression can be ignored. Variables for fixed effects or clusters are often only described in the table notes. For them also provide a mapping here.",
        "items": {
          "type": "object",
          "properties": {
            "label_in_doc": {
              "type": "string",
              "description": "The label of the variable as shown in the document: in the table or used in the text of the article."
            },
            "var_in_code": {
              "type": "string",
              "description": "The variable name as used in the code. If the regressor is an interaction effect between to variables, e.g. 'x' and 'z' use the notation 'x#z'."
            },
            "var_type": {
              "type": "string",
              "description": "A short characteriztion of the variable type: 'd' for dependented variable, 'x' for explanatory variable, 'fe' for a fixed effect, 'clu' for a variable that determines the cluster robust standard errors."
            }
          }
        }
      },
      "error_in_prompt_or_media": {
        "type": ["string", "null"],
        "description": "Is there some inconsistency in the prompt or the attached media files, e.g. the media don't show the tables listed in the prompt etc. This could be an indicator for some error in my pipeline. If such an inconsistency exists, briefly describe it only for the first regression. If all seems ok return null. In other regressions than the first regressions, always set this field to null."
      }
    }
  }
} 
